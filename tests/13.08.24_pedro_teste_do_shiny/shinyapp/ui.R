library(shiny)

# Define UI for miles per gallon application
shinyUI(pageWithSidebar(
  
  # Application title
  headerPanel("Uniconf - Analise de Confiabilidade"),
  
  sidebarPanel(),
  
  mainPanel()
))